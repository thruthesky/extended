name: extended
description: A set of handy widgets and functions that are extended based on Material and Cupertino widgets.
version: 0.1.9
homepage: https://github.com/thruthesky/extended

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^4.0.4

  cached_network_image: ^3.2.0

  path_provider: ^2.0.2

  share_plus: ^3.0.4
  rxdart: ^0.27.3

  # flutter_html for extended text.
  # flutter_html 2.x has dependency problem with fluter_svg
  flutter_html: ^3.0.0-alpha.2

  # for extended text.
  url_launcher: ^6.0.20

  # for extended text.
  flutter_linkify: ^5.0.2
  


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

