import 'package:flutter/material.dart';

class TappableColumn extends StatelessWidget {
  const TappableColumn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
